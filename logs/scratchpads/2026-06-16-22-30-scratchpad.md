# Continuity Scratchpad — 2026-06-16 22:30

## What this session did
Applied the five approved Phase-1 research-layer changes (A–E) from `outputs/research-layer-implementation-plan.md` to the two dossier reference files. Auto mode (picked menu item #1, carryover from the prior plan session). All edits are prompt/synthesis-text extensions of existing blocks; zero new `dossier-data.json` schema fields, no `build.mjs` change. Committed as `48d98b7` (NOT pushed).

## What landed (commit 48d98b7)
- **Change A** — `references/subagent-prompts.md`, anti-tourist guardrail boilerplate: each kept place must now also name its enabling condition (right season, market day, last bus exists) or be dropped; added inspiration-never-decides line. Shared boilerplate → propagates to all list passes.
- **Change B** — `subagent-prompts.md`, Prompt 3 (Pass 3 activities): reframed from "attractions" to "best lived experiences" + search-phrasing guidance. Count/slots/fields unchanged.
- **Change C** — destination thesis: added a 2–4 point thesis task line to Prompt 2a + 2b, and amended `references/dossier-workflow.md` Step 11 intro bullet to open each stop's `intro` with that thesis. Reuses existing `intro` field.
- **Change D** — `subagent-prompts.md`, `Best time` per-place field: now also requests the crowd-mitigation window. (Also lands in Pass 4 food prompt — expected.)
- **Change E** — `subagent-prompts.md`, `Unverified` per-place field: now flags permission/booking needs + non-obvious/seasonal access → routes into existing `rs: needs` worklist.

## Verification done
- All six cited blocks verified present in live files before editing (line numbers had not drifted materially).
- Zero-schema-change invariant confirmed: `intro`, `bestTime`, `rs`/`rsNote` all already exist in `dossier-template.md`.
- Diff contained to the two intended files (31 insertions, 9 deletions).

## One open watch-item (carried from the plan)
Change A's "drop the place" instruction could thin long lists on the next real-trip dry-run. If lists come back sparse, soften to "flag as low-confidence" rather than dropping. Tuning axis, not acted on now.

## Resume With
1. The research-layer plan is now fully implemented — no build session remains. Next real-trip dossier run is the dry-run validation; if long lists come back thin, apply the Change-A softening above.
2. Carryover from the Albania/Balkans trip prep (still open, NOT touched this session): §11 route decision (keep-provisional vs destination-based); pre-travel Perplexity check on Veli Bej / Cogwheel + Children's Railway timetables; optional trip-context date scrub; optional coordinates for new Budapest hero picks.
3. Also unbuilt: the `/daily-program` in-trip workflow spec at `outputs/daily-program-workflow-spec.md` (Phase 2, not started).

## Open questions
- None blocking.
