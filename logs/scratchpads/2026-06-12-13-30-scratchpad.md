# Session Scratchpad — 2026-06-12 (S2)

## What this session did
**v4.3 Mobile UX-PASS fix round** for the Balkans dossier (`trips/balkans-2026-06/`).
The operator dropped `UX-PASS-MOBILE.md` — an 11-finding iPhone 16 Pro pass
(M1–M3 Major, M5–M11 Minor, M4 parked behind a device check) — and the session
implemented the 10 in-scope fixes in the document's fix order
(M1→M2→M3→M11→M5→M6→M7→M8→M9→M10). Committed as `992868d`. Independent QC: **GO**.

### The 10 fixes (all in `index.html` unless noted)
- **M1** — `.notefield` font-size 14→16px (kills iOS focus-zoom on the note field).
- **M2** — `navigator.storage?.persist?.()` at boot (try/catch) + one About
  sentence: marks live on this phone, add to Home Screen, Export as backup.
  Only non-CSS addition (~6 lines).
- **M3** — second full-width ≥48px `.backlink.btm` at the bottom of card pages,
  both normal AND avoid variants of `renderCardPage` (thumb zone; standalone
  swipe-back unreliable for hash history).
- **M11** — `if('scrollRestoration' in history)history.scrollRestoration='manual'`
  at boot (kills one-frame double scroll-jump on back).
- **M5** — `.view`/`.tnrow`/`.tn-menu`/`.subbar` right padding now uses
  `safe-area-inset-right` via 4-value shorthand (landscape island clearance).
- **M6** — `.toolbar button:hover` + `.tn-tab:hover` wrapped in `@media (hover:hover)`.
- **M7** — `touch-action:manipulation` on `.act,.chip,.cl-item,.verify,.tn-tab,.card.row,.toolbar button,.bookbtn,.copybtn`.
- **M8** — `-webkit-user-select:none;user-select:none` on `.card.row,.act`;
  `.coords` keeps `user-select:all`.
- **M9** — `.view.active{animation:none}` added to the reduced-motion block.
- **M10** — `.rpin` min 40→44px both axes; left margin −2px→+4px (separation
  from the `.rnm` navigate area; mis-tap exits to Google Maps).

## Verification
- jsdom harness at `/tmp/balkans-v4-smoke/` (survived from S1).
- **153 assertions green:** test-v4.mjs 61 + test-v4b.mjs 15 + test-v4c.mjs 36
  (its F8b cache check updated v4-4→v4-5) + NEW test-v4d.mjs 41 (M-series:
  M2/M3/M11 behavioural — boot, two backlinks on card + avoid pages, bottom
  link navigates back; CSS fixes as source checks; jsdom lacks scrollRestoration
  so that check is guarded).
- Independent `qc-reviewer`: **GO** — spec fidelity, scope, regressions vs
  F-series, boot ordering, site/ sync all clean.

## Artifacts
- New: `trips/balkans-2026-06/UX-PASS-MOBILE.md` (round spec, mojibake restored,
  Status: M1–M3/M5–M11 RESOLVED, M4 PARKED), `/tmp/balkans-v4-smoke/test-v4d.mjs`.
- Modified: `index.html` (10 fixes), `sw.js` (cache → `balkans-v4-5`),
  `CHANGES.md` (new "# v4.3 — Mobile UX-PASS fix round" section),
  `site/index.html` + `site/sw.js` (re-synced byte-identical).

## Known carryovers (unchanged from S1)
- travel-os git remote broken (`patriklindeberg75-boop/travel-os` not found) —
  push blocked; now 3+ unpushed commits.
- QC note (new, out of scope this round): live `footer` rule still pads its
  right edge from `safe-area-inset-left` (3-value shorthand) — same landscape
  bug M5 fixed in the 4 named containers; candidate for a future round.
- **M4 parked:** status-bar style vs dark theme needs a real-device check
  before choosing a fix.
- Truncated "- make …" feedback item (operator to supply); card-description
  research pass (top ~15); 3 Belgrade food spellings; TODO-PRICE (15);
  coords long tail (104).

## Resume With
The v4.3 mobile round is shipped + committed (`992868d`); next is operator-side:
upload `trips/balkans-2026-06/site/` to Cloudflare and eyeball on iPhone —
M1/M3/M5/M10 are feel-on-device fixes, and the same session should settle the
parked M4 status-bar question (dark theme + standalone: is the clock readable?).
If continuing build work instead: card-description research pass, or the footer
safe-area-right quirk + M4 as a small follow-up round.
