# Session Scratchpad — 2026-06-10 (S2)

## What this session did
- Ran a **diagnose-only UX pass** on the Balkans dossier HTML (`trips/balkans-2026-06/index.html`), mobile-Safari / iPhone-16-Pro lens, internet-available, priority jobs A (near me now) / D (eat tonight) / E (track done/want).
- Produced two docs; QC'd the diagnosis (GO); committed. Then rescoped the workflow doc into V1/V2 on operator pushback; committed again.
- Saved a design-principle memory.

## State on disk (committed)
- `7d21d0d` — ux-diagnosis.md + ux-workflow-additions.md (initial)
- `bbaf027` — rescope workflow-additions into V1/V2 + diagnosis F4 reconcile
- (unpushed — remote still broken, see below)

## Files created
- `trips/balkans-2026-06/ux-diagnosis.md` — verdict (strong pre-trip briefing, read-only prototype for in-trip use) + 13 severity-triaged findings tied to jobs A/D/E. MUST-FIX: F1 no map links, F2 no location signal, F3 no saved state, F4 no open-now.
- `trips/balkans-2026-06/ux-workflow-additions.md` — V1 (hard reqs: map_link, city, neighborhood, stable per-place id) vs V2 roadmap (saved-state, filters, search, structured hours/open-now, time-of-day, app polish, mobile hygiene). Core finding: template spec is already ahead of data + renderer.
- Memory: `dossier-v1-actionable-not-app.md` + MEMORY.md index line.

## Key decisions
- v1 = make places **actionable** (locatable + tappable), NOT an interactive app. Structured hours too fragile for v1 → optional free-text `hours_note`; defer open-now to V2. (Operator second opinion.)
- Renderer fixes stay Patrik's HTML (workflow doesn't touch the shell, per dossier-workflow.md:395); workflow's job is to guarantee the DATA carries what features need.

## Open items / carryover (NOT done this session — out of scope)
- ⚠ travel-os git remote still broken — push as `patriklindeberg75-boop` (not axcioncapital). Multiple commits unpushed.
- Confirm/correct the 3 interpreted Belgrade food spellings in destination-dossier.md (carryover from S1).
- Stage/commit the 14 untracked `.claude/commands/` + `.claude/agents/` files (synced infra, never committed).

## Resume With
Either (a) run the **fix session** on the Balkans dossier — start with V1 data work (workflow emits id/city/neighborhood/map_link) which unblocks the renderer; OR (b) fix the travel-os git remote and push the backlog of unpushed commits.
